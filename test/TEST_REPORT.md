# Circle 测试报告

- 生成时间：2026/8/16 18:47:49
- 用例总数：36，通过：36，失败：0，跳过：0
- 总耗时：52.7s

## 环境信息

- 模型：deepseek / deepseek-v4-flash
- LLM 端到端用例：启用
- Node：v24.13.0

## 结论

全部用例通过。三个验收用例（长程任务 / 定时任务 / 安全拦截）均满足预期结果。

## 用例明细

| ID | 分类 | 用例 | 结果 | 耗时 |
| --- | --- | --- | --- | --- |
| U-01 | 安全评估 | 破坏性操作（删除运行目录）被识别 | ✅ 通过 | 0.0s |
| U-02 | 安全评估 | rm -rf 被识别 | ✅ 通过 | 0.0s |
| U-03 | 安全评估 | 敏感信息（读取 SSH 私钥）被识别 | ✅ 通过 | 0.0s |
| U-04 | 安全评估 | 敏感信息（读取 .env / API Key）被识别 | ✅ 通过 | 0.0s |
| U-05 | 安全评估 | 正常请求不被误拦截 | ✅ 通过 | 0.0s |
| U-06 | 安全评估 | 否定语境（不要读取密钥）不误拦截 | ✅ 通过 | 0.0s |
| U-06b | 安全评估 | 持久定时/后台驻留机制被识别（Worker 越权自建定时器） | ✅ 通过 | 0.0s |
| U-06c | 安全评估 | 合法的定时任务创建请求不被误拦截 | ✅ 通过 | 0.0s |
| U-07 | cron | 标准 5 段表达式解析 | ✅ 通过 | 0.0s |
| U-08 | cron | nextRun 计算（每天本地 10 点） | ✅ 通过 | 0.0s |
| U-09 | cron | nextRun 步长表达式（每 5 分钟） | ✅ 通过 | 0.0s |
| U-10 | cron | 非法表达式抛错 | ✅ 通过 | 0.0s |
| U-11 | cron | matches 判定（清理 cron 本地 03:00） | ✅ 通过 | 0.0s |
| U-11b | cron | nextRun exclusive：严格晚于 from，防止同一分钟重复触发 | ✅ 通过 | 0.0s |
| U-12 | 任务存储 | 创建 / 状态流转 / 待办查询 | ✅ 通过 | 0.0s |
| U-13 | 任务存储 | 清理超过 30 天的已完成任务 | ✅ 通过 | 0.0s |
| U-13b | 任务存储 | 启动对账：进程重启遗留的进行中任务标记为失败 | ✅ 通过 | 0.0s |
| U-18 | cron | issue#1 回归：本地 09:00 触发，不再晚 8 小时（nextRun） | ✅ 通过 | 0.0s |
| U-19 | cron | issue#1 回归：matches 按本地时区判定 | ✅ 通过 | 0.0s |
| U-14 | 定时任务存储 | 创建 / 更新 / 删除 | ✅ 通过 | 0.0s |
| U-14c | 汇报摘要 | 长文本头尾兼顾，短文本原样返回 | ✅ 通过 | 0.0s |
| U-15 | 工作空间 | Worker 独立目录 + 任务级会话工作空间隔离 + 产出物归档 | ✅ 通过 | 0.0s |
| U-16 | Scheduler | 创建定时任务并立即触发 | ✅ 通过 | 0.0s |
| U-17 | IM 适配器 | TestAdapter 注入与等待 | ✅ 通过 | 0.0s |
| U-20 | 微信官方通道 | 扫码登录：二维码 → 轮询 → confirmed → 保存账户 | ✅ 通过 | 6.0s |
| U-21 | 微信官方通道 | 消息接收：getupdates → onMessage 回调 | ✅ 通过 | 0.5s |
| U-22 | 微信官方通道 | 消息发送：sendmessage + markdown 清洗 | ✅ 通过 | 3.0s |
| U-23 | 微信官方通道 | 缓存恢复：重启后不重复扫码 | ✅ 通过 | 5.0s |
| C1-01 | 用例1: 长程任务 | 用户提交长程任务：收到确认 → 派发 → 执行 → 汇报 | ✅ 通过 | 22.6s |
| C1-02 | 用例1: 短程任务 | 用户提交短程任务：直接返回结果（无递归汇报 bug） | ✅ 通过 | 4.5s |
| C2-01 | 用例2: 定时任务 | 创建定时任务 → 触发执行 → 结果回流 → 删除 | ✅ 通过 | 9.3s |
| C3-01 | 用例3: 安全拦截 | 破坏性请求（删除运行目录）被拒绝且不派发 | ✅ 通过 | 0.0s |
| C3-02 | 用例3: 安全拦截 | 敏感请求（读取 SSH 私钥 / .env）被拒绝且不派发 | ✅ 通过 | 0.0s |
| C3-03 | 用例3: 安全拦截 | 派发入口二次校验（绕过 Coordinator 也无效） | ✅ 通过 | 0.0s |
| C3-04 | 用例3: 安全拦截 | 拦截后系统仍可正常对话（白名单不误伤） | ✅ 通过 | 0.0s |
| C4-01 | 用例4: 重启对账 | 启动时标记中断任务为失败并通知用户（仅通知，不自动恢复） | ✅ 通过 | 1.7s |

## 失败/跳过详情

## 运行详情（含真实对话记录）

### U-01 破坏性操作（删除运行目录）被识别

```
判定: destructive, 原因: 破坏性操作: 删除运行目录/项目文件; 破坏性操作: 删除目录
```

### U-02 rm -rf 被识别

```
通过
```

### U-03 敏感信息（读取 SSH 私钥）被识别

```
判定: sensitive, 原因: 敏感信息: SSH 私钥文件; 敏感信息: SSH 配置目录
```

### U-04 敏感信息（读取 .env / API Key）被识别

```
通过
```

### U-05 正常请求不被误拦截

```
通过
```

### U-06 否定语境（不要读取密钥）不误拦截

```
通过
```

### U-06b 持久定时/后台驻留机制被识别（Worker 越权自建定时器）

```
「创建 crontab 每 5 分钟报时」→ 破坏性操作: crontab 定时任务表（系统级定时器）
「执行 crontab -e 添加定时任务」→ 破坏性操作: crontab 定时任务表（系统级定时器）
「写个定时脚本每天凌晨清理日志」→ 破坏性操作: 自建定时脚本
「创建 cron 脚本每分钟运行」→ 破坏性操作: 自建 cron/crontab 定时条目
「用 systemctl enable 启动定时服务」→ 破坏性操作: systemd 服务/定时器管理
「nohup 后台运行脚本」→ 破坏性操作: nohup 后台驻留运行
「用 at now 安排一次性任务」→ 破坏性操作: at 一次性定时任务
```

### U-06c 合法的定时任务创建请求不被误拦截

```
通过
```

### U-07 标准 5 段表达式解析

```
通过
```

### U-08 nextRun 计算（每天本地 10 点）

```
通过
```

### U-09 nextRun 步长表达式（每 5 分钟）

```
通过
```

### U-10 非法表达式抛错

```
通过
```

### U-11 matches 判定（清理 cron 本地 03:00）

```
通过
```

### U-11b nextRun exclusive：严格晚于 from，防止同一分钟重复触发

```
通过
```

### U-12 创建 / 状态流转 / 待办查询

```
通过
```

### U-13 清理超过 30 天的已完成任务

```
通过
```

### U-13b 启动对账：进程重启遗留的进行中任务标记为失败

```
通过
```

### U-18 issue#1 回归：本地 09:00 触发，不再晚 8 小时（nextRun）

```
nextRun("0 9 * * *", 本地 00:00) → 2025/1/1 09:00:00（本地 09:00，不再晚 8 小时）
```

### U-19 issue#1 回归：matches 按本地时区判定

```
matches 已按本地时区判定，旧 UTC 误匹配已消除
```

### U-14 创建 / 更新 / 删除

```
通过
```

### U-14c 长文本头尾兼顾，短文本原样返回

```
通过
```

### U-15 Worker 独立目录 + 任务级会话工作空间隔离 + 产出物归档

```
通过
```

### U-16 创建定时任务并立即触发

```
通过
```

### U-17 TestAdapter 注入与等待

```
通过
```

### U-20 扫码登录：二维码 → 轮询 → confirmed → 保存账户

```
通过
```

### U-21 消息接收：getupdates → onMessage 回调

```
通过
```

### U-22 消息发送：sendmessage + markdown 清洗

```
通过
```

### U-23 缓存恢复：重启后不重复扫码

```
通过
```

### C1-01 用户提交长程任务：收到确认 → 派发 → 执行 → 汇报

```
[3.5s] Coordinator 确认: 好的，我来派发这个长程任务给 dev Worker。任务已派发，编号 **T-20260816-0001**。

该任务包含 12 秒的 sleep，属于长程任务，我已在完成后第一时间向你汇报。请稍候，我会跟进执行结果。…
[3.5s] 任务已创建: T-20260816-0001（priority=long）
[22.5s] 最终汇报已收到: 任务 **T-20260816-0001** 已完成 ✅

## 执行结果

**任务内容**
- 编写了 bash 脚本 `run_task.sh`，依次执行：`sleep 12` → 追加写入字符串 → 读取文件内容。

**产出文件**
- 脚本：`/tmp/circle-case1-GZK5lt/workspaces/dev/tasks/T-20260816-0001/run_task.s…
产出物: /tmp/circle-case1-GZK5lt/workspaces/dev/outputs/T-20260816-0001/done.txt → LONG_TASK_DONE_2025
共 2 条回复，全部经由 Coordinator 转发。
```

### C1-02 用户提交短程任务：直接返回结果（无递归汇报 bug）

```
[4.5s] 短程任务结果: ✅ **任务完成**

**任务编号：** T-20260816-0001「写入并读取 short.txt」

**执行结果：**
- 已执行 `echo SHORT_OK_2025 > short.txt` 写入文件；
- 读取到文件内容为：`SHORT_OK_2025`

**产出物位置：** `/tmp/circle-case1-short-sDlwsO/workspaces/dev/tas…
短程任务状态已标记 completed。
```

### C2-01 创建定时任务 → 触发执行 → 结果回流 → 删除

```
[1.5s] 定时任务已创建: S-MSVOL1M4-001「每日状态检查」cron="0 10 * * *"
[1.5s] 手动触发定时任务（模拟到达触发时间）…
[8.2s] 定时任务结果已回流: 我来为您创建这个定时任务。定时任务已创建成功 ✅

- **任务名称**：每日状态检查
- **任务 ID**：S-MSVOL1M4-001
- **cron 表达式**：`0 10 * * *`（每天上午 10:00）
- **执行 Wo…
任务 T-20260816-0001 已完成，Scheduler 已记录触发。
[9.2s] 定时任务 S-MSVOL1M4-001 已删除。
```

### C3-01 破坏性请求（删除运行目录）被拒绝且不派发

```
拦截回复: ⚠️ 安全拦截：该请求涉及破坏性操作，已被拒绝执行。
Coordinator 不会执行任何删除、格式化、关停等破坏性操作，也不会将其派发给 Worker 或 S…
```

### C3-02 敏感请求（读取 SSH 私钥 / .env）被拒绝且不派发

```
拦截回复: ⚠️ 安全拦截：该请求涉及敏感信息（密钥/口令/私钥等），已被拒绝读取与返回。
Coordinator 不会访问或返回敏… / ⚠️ 安全拦截：该请求涉及敏感信息（密钥/口令/私钥等），已被拒绝读取与返回。
Coordinator 不会访问或返回敏…
```

### C3-03 派发入口二次校验（绕过 Coordinator 也无效）

```
派发拦截: ⚠️ 安全拦截：该请求涉及破坏性操作，已被拒绝执行。
Coordinator 不会执行任何删除、格式… / ⚠️ 安全拦截：该请求涉及敏感信息（密钥/口令/私钥等），已被拒绝读取与返回。
Coordinato…
```

### C3-04 拦截后系统仍可正常对话（白名单不误伤）

```
正常对话回复: ⚠️ 安全拦截：该请求涉及破坏性操作，已被拒绝执行。
Coordinator 不会执行任何删除、格式化、关停等破坏性操作，也不会将其派发给 Worker 或 Scheduler。
如确需此类操作，请由…（第 0 条）
```

### C4-01 启动时标记中断任务为失败并通知用户（仅通知，不自动恢复）

```
中断通知: 收到系统通知。以下是需要向你说明的情况：

## ⚠️ 任务中断提醒

上次系统重启时，有 **1 个任务** 因进程中断未能完成，已被标记为失败：

| 任务编号 | 任务名称 | Worker | 状态 |
|---------|---------|--------|------|
| T-LEGACY-0001 | 重启前未完成的长程任务 | dev | ❌ 失败（中断） |

---

如…
```
